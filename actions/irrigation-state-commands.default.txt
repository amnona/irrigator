[state_commands]
# state commands affecting all timers/faucets in all computers and are not deleted after read

# disable a specific computer
# disable_computer = COMPUTERNAME

# set leak monitoring mode (true/false)
# monitor_leaks = false

# increase/decrease irrigation times for all timers by a given percent. needs to end with %
# set_percent = 100%

# disable one or more irrigation lines (comma-separated)
# disable_line = LINE_NAME1,LINE_NAME2

# disable one or more fertilization pumps (comma-separated)
# disable_fertilization = PUMP_NAME1,PUMP_NAME2

# set to manual mode or auto mode
# in manual mode, we ignore the weekly timers and just use manual commands
# mode = manual
